{
  "name": "backstop-failsonly",
  "version": "0.0.1",
  "description": "",
  "main": "dist/error-report-only.sh",
  "scripts": {
    "serve": "webpack serve --config webpack.config.js --progress --profile --content-base src",
    "build": "webpack build --config webpack.config.js --progress --profile  && chmod +x dist/error-report-only.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.24.3",
    "webpack-cli": "^4.5.0",
    "webpack-dev-server": "^3.11.2"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "recursive-copy": "^2.0.11",
    "yargs": "^16.2.0"
  }
}
